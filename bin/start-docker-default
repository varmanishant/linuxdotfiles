#!/usr/bin/dash

docker run \
    --env DISPLAY=$DISPLAY \
    --interactive \
    --rm \
    --network host \
    --privileged \
    --tty \
    --volume $HOME/.Xauthority:$HOME/.Xauthority \
    --volume $HOME:$HOME \
    "${@}"
