#!/usr/bin/env python3

import os
import pathlib
import runpy

home = os.environ['HOME']
apps = os.environ['APPS']

kitty = os.path.join(home, apps, 'kitty')

runpy.run_path(kitty, run_name='__main__')
