# Launch

# TODO: Should this folder be owned by the user?
acme -m /mnt/acme .acme

# Tips

1. Selecting brackets will select the content inside it. This can be useful for
   grouping commands. But it is a good idea to create a single command for
   commonly used functions.
2. The escape key (ESC) will select the content typed since last mouse action.
   If it is pressed again, it will cut the text. This is very handy in `win` to
   re-execute a command.
3. `Edit =` will show the current line number.
4. Scroll wheels can be used to expand and collapse `tags`.
5. You can middle-click on the filename to execute it; it will respect the
    shebang header of the file.
6. Fonts can be changed using the `fontsrv` command.
7. unicode.8x13B.font can be used as the monospace font.

# Commands

9    fd    Font    g    "Indent on"    src    wc    win    winwatch

"|fmt -w 79"    |sort

cleanup-system    start-docker    start-terminal    stop-docker

update-system

stop-docker ; cleanup-system ; lc /home/nishant/Downloads/
echo Acme | xclip -i

# Files

linuxdotfiles/.clang-format
linuxdotfiles/.rcrc
linuxdotfiles/plan9.txt
notes/the-zen.md
scratch/scratch.c
scratch/scratch.md
scratch/scratch.py
scratch/scratch.txt

scratch/plan9/ls.c
scratch/x11/button.c

# Folders

./
linuxdotfiles/
notes/
scratch/
src/
src/2bwm/
src/ed/
src/bmpanel/
src/plan9port/

scratch/sync-directories/

# Git

git-cola
git add -p
git commit -m "Fixups"
git push origin master:master

# Python

python $%    pyflakes    pylint
